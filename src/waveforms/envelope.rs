pub struct Envelope {
    attack_rate: u8,
    decay_attack_rate: u8,
    sustain_level: u8,
    decay_sustain_rate: u8,
    release_rate: u8,

    attack_amplitude: u8,
    sustain_amplitude: u8,
}

impl Envelope {}
